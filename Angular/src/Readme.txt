/* You can add global styles to this file, and also import other style files */
()-event binding syntax is used to pass the data from html to css
[]-property binding is used to passed the data from ts to html
[()]-this is called as banana in box syntax which is used for two way data binding.
But in order to use Two way databinding we have to use buildin module known as Forms module
Two way databinding is very important feature for angular and very popular .
All forms submission is typically done in two way data binding syntax:
[(ngModel)]="username"
where ngModel is buildin attribute gven by angular.
username is defined 